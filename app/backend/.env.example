# Environment variables — copy to .env and fill in your values
# DO NOT commit the actual .env file

# HuggingFace — Behavioral Classifier Model
HF_TOKEN=
HF_MODEL_ID=

# AI APIs (optional — notes assembly doesn't need these)
GROQ_API_KEY=
GEMINI_API_KEY=

# App Secret (change in production)
SECRET_KEY=medha-dev-secret-change-in-prod
